import React from 'react';

const QuestionAns = () => {
    return (
        <div>
            <h2>what is context api?</h2>
            <p>context api is a react structure via that we can pass data from grandparent to child to parent without props drilling.
                we get provider and consumer we pass by provider and get by consumer .three step we use context api
                1. create a context (const ContextName = createContext('') and then export)
                2. wrap parent with context provider and provider a value.
                3. consumer that value</p>
            <h2>what is symantics tag?</h2>
            <p>symantics tag that tag they has a meaning.symantics tag .they discribe its meaning the browser and the developer both.
                Examples : header, footer, section, article, form, nav, main, etc.</p>
                </div>
                    );
};

                    export default QuestionAns;